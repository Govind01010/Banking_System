<!DOCTYPE html>
<html lang="en">
<head>

    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.0.2/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-EVSTQN3/azprG1Anm3QDgpJLIm9Nao0Yz1ztcQTwFspd3yD65VohhpuuCOmLASjC" crossorigin="anonymous">

    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.0.2/dist/js/bootstrap.bundle.min.js" integrity="sha384-MrcW6ZMFYlzcLA8Nl+NtUVF0sA7MsXsP1UyJoMp4YLEuNSfAP+JcXn/tWtIaxVXM" crossorigin="anonymous"></script>

  <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.9.1/font/bootstrap-icons.css" />


  <style>
    .modal-header{
      padding:0.5rem 1rem;
    }
    li{
      list-style: none;
    }
  </style>
    <title>Document</title>
</head>
<body>

 


    <div class="header d-flex justify-content-between m-3">
        <h6 class="text-center"><i class="bi bi-credit-card"></i>     TRANSACTION HISTORY </h6>
        <div class="header-left d-flex">
              <div class="path"><a href="/" class="btn btn-primary pb-0 pt-0 "><i class="bi bi-house-door-fill"></i> Home</a></div>
        </div>
    </div>

    <table border="1"  class="table table-striped table-dark table-hover table-sm fs-15 mt-2 m-auto"
      <thead class="thead-light">
        <tr>
           
            <th>Sender Name</th>
            <th>reciver Name</th>
            <th>Transfer amount</th>
            <th class="text-center">TIME</th>
            <td>ACTION</td>
        </tr>
      </thead>

        <% if(transaction.length>0){ %>
            <% for(var i=0 ; i<transaction.length ; i++ ){ %>
                <tr>
                    <td><%- transaction[i].sender %></td>
                    <td><%- transaction[i].reciver %></td>
                    <td><%- transaction[i].amount %><i class="bi bi-currency-rupee"></i></td>
                    <td><%- transaction[i].Date %></td>

                    <td><a href="/delete-transaction?txn_id=<%- transaction[i].txn_id %>" class="btn btn-danger"><i class="bi bi-trash"></i></i></a></td>  
                </tr>
                <% } %>
        <% }else{ %>
            
            <h1>Transactions is not happend yet !</h1>
            <% }  %>
    </table>

</body>
<script>
   function senddetails(){
     var currentdate = new Date();
     var datetime = "Last Sync: " + currentdate.getDay() + "/" + currentdate.getMonth() 
     + "/" + currentdate.getFullYear() + " @ " 
     + currentdate.getHours() + ":" 
     + currentdate.getMinutes() + ":" + currentdate.getSeconds();
    }
    
    document.getElementById("txn_time").innerHTML=datetime;
   
</script>
 </html>